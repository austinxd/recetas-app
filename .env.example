# ================================
# CONFIGURACIÓN PARA SERVIDOR REMOTO
# Sistema de Gestión de Cocina Django
# ================================

# Django Configuration
DEBUG=False
SECRET_KEY=tu_clave_secreta_super_segura_aqui_cambiar_obligatorio
ALLOWED_HOSTS=tu-dominio.com,www.tu-dominio.com,IP_DEL_SERVIDOR

# Database Configuration - PostgreSQL (Recomendado)
DATABASE_URL=postgresql://usuario:contraseña@localhost:5432/kitchen_db
DB_ENGINE=django.db.backends.postgresql
DB_NAME=kitchen_db
DB_USER=tu_usuario_db
DB_PASSWORD=tu_contraseña_db
DB_HOST=localhost
DB_PORT=5432

# Database Configuration - MySQL (Alternativa)
# DB_ENGINE=django.db.backends.mysql
# DB_NAME=kitchen_db
# DB_USER=tu_usuario_mysql
# DB_PASSWORD=tu_contraseña_mysql
# DB_HOST=localhost
# DB_PORT=3306

# Security
SESSION_SECRET=otra_clave_secreta_para_sesiones_cambiar_obligatorio

# Static Files (para producción)
STATIC_URL=/static/
STATIC_ROOT=/var/www/tu_app/static/
MEDIA_URL=/media/
MEDIA_ROOT=/var/www/tu_app/media/

# CORS Configuration (si usas API desde otro dominio)
CORS_ALLOWED_ORIGINS=https://tu-dominio.com,https://www.tu-dominio.com

# Email Configuration (opcional)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tu_email@gmail.com
EMAIL_HOST_PASSWORD=tu_contraseña_app_gmail

# Cache Configuration (opcional - Redis)
# REDIS_URL=redis://localhost:6379/1

# Logging Level
LOG_LEVEL=INFO

# Time Zone
TIME_ZONE=Europe/Madrid

# Language
LANGUAGE_CODE=es-es